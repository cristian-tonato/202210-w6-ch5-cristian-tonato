import styles from "../header/header.module.css";

export function Footer() {
    return (
        <footer className={styles.header}>
            <address>COMIC-GAME</address>
        </footer>
    );
}
